#!/usr/bin/env node

if(process.argv.length < 3){
  throw new Error('Usage: $ admindo /path/to/metadata.yml');
}

var Megagear = require('../index');
var megagear = new Megagear(process.argv[2]);

megagear.admindo(process.env, function(err){
  if(err) throw err;
});
