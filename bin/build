#!/usr/bin/env node

if(process.argv.length < 3){
  throw new Error('Usage: $ build /path/to/metadata.yml');
}

var Megagear = require('../index');
var megagear = new Megagear(process.argv[2]);

megagear.build(process.env, function(err){
  if(err) throw err;
});
